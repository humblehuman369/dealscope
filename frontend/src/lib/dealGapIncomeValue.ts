/**
 * Live Deal Gap Income Value from worksheet state.
 * Re-exported here so app routes can import without touching deprecated `estimateIncomeValue` path rules.
 */

export {
  computeDealGapIncomeValue,
  type DealGapOperatingOverrides,
} from '@/utils/estimateIncomeValue'
