'use client'

// PropertyOverview is no longer needed as the summary cards and toggle
// are now in WorksheetHeader. This component can be removed from the layout.
// Keeping it as an empty export for backward compatibility.

export function PropertyOverview() {
  // All functionality moved to WorksheetHeader
  return null
}
