/**
 * @dealscope/shared — Single Source of Truth
 *
 * Shared types and constants consumed by both frontend (Next.js) and mobile (Expo).
 *
 * Usage (after configuring path aliases):
 *   import type { StrategyId, DealMakerRecord } from '@dealscope/shared';
 *   import { STRATEGY_CONFIG, DEFAULT_ASSUMPTIONS } from '@dealscope/shared';
 */

// Types
export * from './types';

// Constants
export * from './constants';
