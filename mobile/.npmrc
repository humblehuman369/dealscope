## Prevent npm from hoisting to or reading from the monorepo root.
## Required for EAS builds where mobile and frontend have different React versions.
install-strategy=nested

## EAS Build: bypass strict peer dependency resolution (common fix for "Install dependencies" failures)
legacy-peer-deps=true
