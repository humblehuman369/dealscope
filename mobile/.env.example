# DealGapIQ Mobile — Environment Variables
# Copy to .env and fill in values.

# Backend API base URL (no trailing slash)
EXPO_PUBLIC_API_URL=http://localhost:8000

# Google Places API key (for address autocomplete)
EXPO_PUBLIC_GOOGLE_PLACES_API_KEY=

# PostHog Analytics (optional — analytics disabled if key is empty)
EXPO_PUBLIC_POSTHOG_API_KEY=
EXPO_PUBLIC_POSTHOG_HOST=https://us.i.posthog.com

# RevenueCat (falls back to test key if platform keys are empty)
EXPO_PUBLIC_REVENUECAT_APPLE_KEY=
EXPO_PUBLIC_REVENUECAT_GOOGLE_KEY=
# Test/sandbox key (used automatically when platform keys are empty)
# EXPO_PUBLIC_REVENUECAT_TEST_KEY=test_xzbytpAbtdKvskFqkECPaqULtZO

# Sentry (optional — crash reporting disabled if DSN is empty)
EXPO_PUBLIC_SENTRY_DSN=
