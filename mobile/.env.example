# ─────────────────────────────────────────────────────────────
# DealGapIQ Mobile — Environment Variables
# ─────────────────────────────────────────────────────────────
# Copy to .env and fill in values. Variables prefixed with
# EXPO_PUBLIC_ are bundled into the client JS and visible
# to end-users; never put secrets there.
#
# For EAS builds, add secrets via:
#   eas secret:create --name KEY --value VALUE --scope project
# ─────────────────────────────────────────────────────────────

# ── API ─────────────────────────────────────────────────────
# Backend base URL (no trailing slash)
EXPO_PUBLIC_API_URL=https://dealscope-production.up.railway.app

# Web app URL (used for universal links / sharing)
EXPO_PUBLIC_WEB_APP_URL=https://investiq.guru

# ── Maps ────────────────────────────────────────────────────
# Google Maps API key (restricted to iOS + Android app bundles)
EXPO_PUBLIC_GOOGLE_MAPS_API_KEY=

# ── Monitoring ──────────────────────────────────────────────
# Sentry DSN for crash/error reporting
EXPO_PUBLIC_SENTRY_DSN=

# Sentry auth token — set as EAS secret, NOT here
# Used at build time for source-map uploads
# SENTRY_AUTH_TOKEN=
# SENTRY_ORG=investiq
# SENTRY_PROJECT=investiq-mobile

# ── Push Notifications ──────────────────────────────────────
# Expo push access token (optional, improves delivery reliability)
# Set as EAS secret for production builds
# EXPO_ACCESS_TOKEN=

# ── Build Environment ──────────────────────────────────────
# Set automatically by EAS build profiles; override for local
# Values: development | preview | beta | production
# APP_ENV=development
