[build]
builder = "nixpacks"

[deploy]
# Fix alembic version mismatch, then start server
startCommand = "python fix_alembic.py"
healthcheckPath = "/health"
healthcheckTimeout = 300
restartPolicyType = "on_failure"
restartPolicyMaxRetries = 10
